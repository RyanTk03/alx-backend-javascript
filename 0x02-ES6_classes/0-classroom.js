/**
 * A class room representation
 */
export default class ClassRoom {
	/**
	 * contructor of the class
	 *
	 * @param {Number} maxStudentsSize - max number of students in
	 * the class.
	 */
	contructor(maxStudentsSize) {
		this._maxStudentsSize = maxStudentsSize;
	}
}
